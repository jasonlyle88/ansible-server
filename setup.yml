---
- hosts: localhost
  connection: local

  vars_files:
  - vars/main.yml

  roles:
  - role: packages2
    vars:
      jml_type: pacman_main
      jml_packages: "{{ packages.main }}"
  # - firewall
  # - autotime
  # - hostname
  # - packages
  # - snap
  # - users_and_roles
  # - ssh
  # - shell
  # - docker
